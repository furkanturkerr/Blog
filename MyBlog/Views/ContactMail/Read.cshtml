@model List<Entities.Concrate.Contact>
@{
    ViewData["Title"] = "İletişim";
    Layout = "_Layout";
}

<div class="page-header">
    <div class="page-block">
        <div class="page-header-title">
            <h5 class="mb-0 font-medium">İletişim</h5>
        </div>
        <ul class="breadcrumb">
            <li class="breadcrumb-item" aria-current="page">Mesajlar</li>
            <li class="breadcrumb-item" aria-current="page">Okunmuş Mesajlar</li>
        </ul>
    </div>
</div>

<div class="container-fluid mt-4">

    <div class="row mb-3">
        <div class="col d-flex gap-2">
            <a href="~/ContactMail/Unread" class="btn btn-dark">📩 Okunmamış Mesajlar</a>
            <a href="~/ContactMail/Read" class="btn btn-outline-dark active">📬 Okunmuş Mesajlar</a>
        </div>
    </div>

    <div class="row">
        <div class="col">
            <div class="card shadow-sm">
                <div class="card-header fw-bold">
                    Mesaj Listesi
                </div>

                <div class="card-body p-0">
                    <table class="table table-hover table-bordered mb-0 align-middle text-center">
                        <thead class="table-dark">
                            <tr>
                                <th>İsim Soyisim</th>
                                <th>E-Mail</th>
                                <th>Detay</th>
                                <th>Durum</th>
                                <th>Sil</th>
                            </tr>
                        </thead>

                        <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="fw-semibold">@item.Name</td>
                                <td>@item.Email</td>

                                <td>
                                    <a href="~/ContactMail/Detail/@item.ContactId"
                                       class="btn btn-sm btn-outline-success">
                                        Detay
                                    </a>
                                </td>
                                
                                <td>
                                    <a href="~/ContactMail/ChangeStatusFalse/@item.ContactId"
                                       class="btn btn-sm btn-outline-warning">
                                        Okunmadı
                                    </a>
                                </td>
                                
                                <td>
                                    <a href="~/ContactMail/Delete/@item.ContactId"
                                       class="btn btn-sm btn-outline-danger">
                                        Sil
                                    </a>
                                </td>
                            </tr>
                        }
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
